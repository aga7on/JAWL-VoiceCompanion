# U6 parity status (2026-09-15)

Сверка после срезов U0-U5. Источник: `registry.json` (свежий прогон).
Политика: удаление iframe ТОЛЬКО после 100% parity — сейчас не удалён.

## Покрыто в едином UI (после срезов U4-U5)

| Зона | Покрытие | Где |
|---|---|---|
| Настройки ядра (identity, llm) | 6 скаляров | Настройки · «Ядро агента» |
| Такт/heartbeat/timezone/ускорение | 7 | Настройки · «Такт и контекст» |
| RAG лимиты + swarm | 3 | Настройки · «Такт и контекст» |
| Память caps + гипотезы + ToT + подсознание | 9 | Настройки · «Память и подсознание» |
| Drives настройки (14 ключей) | 14 | Настройки · «Мотивация» |
| Секреты/интеграции (env 16 полей) | 16 | Настройки · «Интеграции и секреты» |
| Память (list/filter/search/revise/forget/archive + evidence) | редактор | Память |
| Журнал автономности (планы) + сводка in_progress | поверхность + рейл | Память · рейл |
| Логи агента (tail + redaction) | endpoint + карточка | Система |
| Состояние агента (фаза/шаг/сон), фон-сообщения | рейл + чат | рейл |
| Голос (полный конвейер) + Восприятие + Доступ | native | свои вкладки |
| Config hub (ревизии/секреты/бэкапы/readback) | writer | Настройки |

Итого **39 скаляров настроек** перенесено (из 213 контролов).

## Прогресс миграции interfaces (после шага 2)

- Порция 1: **Telegram — 17 полей** (Telethon/Aiogram) — ГОТОВО.
  Framework: типизированные карточки по спецификации (`IFACE_SECTIONS`),
  nullable-поля пишутся только при заполнении (защита StrictInt|None).
- Порция 2: **GitHub/Email/Календарь/Граф кода/Meta — 17 полей** — ГОТОВО.
- Порция 3: **MCP 6 + Debug Broker 9 + Мультимодальность 7 + Облачный голос 14** — ГОТОВО.
- Порция 4: **Web-инструменты 24 + HostOS детали 33** — ГОТОВО (включая
  float-тип для дробных скаляров).
- Итого перенесено: **187/213 контролов (88%)**. Остались: custom-drives
  CRUD (SQLite /api/drives), DB-wipe решение владельца, и списочные
  редакторы консоли (feeds/servers/enabled_providers — console-transitional).

## Финальный срез (2026-09-15)

- **Списковые редакторы (6 строковых списков)** — ГОТОВО: LLM available
  models, code_graph.exclude_dirs, debug_broker.enabled_providers,
  coding_host_allowed_commands, ElevenLabs/Edge available_voices; запись
  через hub lists с round-trip тестом.
- **DB-wipe** — ГОТОВО как CRITICAL-поверхность: слово «УДАЛИТЬ» + нативный
  диалог, роут к собственному эндпоинту консоли (единый owner).
- Parity: **193/213 (91%)**. Остаток — объектные списки (feeds/servers/
  coding-профили) и custom-drives CRUD: сознательно console-transitional,
  iframe сохраняется как rollback-путь до их переноса.

## Оставшиеся до 100% parity

1. `interfaces:*` интеграционные скаляры — **127 полей** (telegram 17, web 24,
   host 33, debug_broker 9, multimodality 7, mcp 6, github 5, calendar/email/
   code_graph/meta по 3, voice 14 — часть voice уже частично покрыта
   companion-native линией ASR/TTS).
2. Drives: **custom drives CRUD** (добавление/удаление своих драйвов) — через
   `/api/drives` update_custom; состояния остаются в SQLite.
3. **DB wipe** — CRITICAL-действие, отдельное решение владельца (двойное
   подтверждение + журналирование), не перенесён сознательно.
4. ToT `llm_model`/`mode`, subconscious llm_model — входили в блоки срезов
   частично; сверить точечно по реестру.

## План закрытия (порядок)

1. Миграция interfaces-групп порциями по 20-30 полей (round-trip тесты на
   каждую порцию, как в срезах 1-4).
2. Custom drives CRUD-карточка.
3. DB-wipe: явное решение владельца (в консоли остаётся).
4. Финальная приёмка: полный реестр-сверка → удаление iframe → browser E2E →
   ночной soak (команда готова в TODO) → пользовательская приёмка.

Rollback до удаления iframe: консоль JAWL остаётся рабочей поверх `/console/`
(transition), никакие данные не дублируются (один writer на каждую зону).
